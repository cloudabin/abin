---
- name: ping
  hosts: all
  gather_facts: no
  
  vars:
    var1: 35,36,80,8080,8081
  tasks:
    - name: portsl
      set_fact:
        list_var1: "{{ var1.split(',') }}"

    - name:
      debug:
        msg: "{{ list_var1 }}"
        #      loop: "{{ list_var1 }}"  
